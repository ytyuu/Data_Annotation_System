import { SectionPlaceholder } from '../shared/SectionPlaceholder';

export function AnnotatorReturnedTasksPage() {
  return (
    <SectionPlaceholder
      title="退回任务"
      description="处理被退回的标注任务。"
    />
  );
}

