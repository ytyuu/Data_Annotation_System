{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src -e kt --exec \"node scripts/dev-api.mjs\"",
    "stop": "node scripts/stop-api-port.mjs",
    "build": "mvnd -q compile",
    "test": "mvnd -q compile",
    "lint": "mvnd -q compile"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
