{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src -e kt --exec \"node scripts/dev-api.mjs\"",
    "stop": "node scripts/stop-api-port.mjs",
    "build": "mvn4 -q -ntp compile",
    "test": "mvn4 -q -ntp compile",
    "lint": "mvn4 -q -ntp compile"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
