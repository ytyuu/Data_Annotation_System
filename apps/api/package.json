{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src -e kt --exec \"mvnd -q compile && mvnd exec:java\"",
    "build": "mvnd -q compile",
    "test": "mvnd -q compile",
    "lint": "mvnd -q compile"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
