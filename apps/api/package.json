{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src -e kt --exec \"powershell -NoProfile -ExecutionPolicy Bypass -File scripts/dev-api.ps1\"",
    "stop": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/stop-api-port.ps1",
    "build": "mvnd -q compile",
    "test": "mvnd -q compile",
    "lint": "mvnd -q compile"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
