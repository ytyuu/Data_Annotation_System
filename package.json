{
  "private": true,
  "name": "data-annotation-system-monorepo",
  "packageManager": "pnpm@11.5.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "pg": "^8.22.0",
    "turbo": "^2.10.0",
    "typescript": "^6.0.3"
  }
}
