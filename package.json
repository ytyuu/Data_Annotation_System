{
  "private": true,
  "name": "data-annotation-system-monorepo",
  "packageManager": "pnpm@10.29.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.7"
  }
}

